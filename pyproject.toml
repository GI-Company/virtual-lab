[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "virtual_lab"
version = "0.1.0"
description = "VirtualLab: Epistemic Research Cockpit"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2,<3",
    "numpy>=2,<3",
    "scipy>=1.15,<2",
    "google-genai>=0.2.2",
    "PyNaCl>=1.5",
    "pint>=0.24"
]

[project.optional-dependencies]
cockpit = [
    "PySide6>=6.11,<7",
    "rdkit>=2025.9",
    "pyqtgraph==0.14.0",
    "zeroconf==0.132.2"
]
dev = [
    "pytest>=8",
    "pytest-qt>=4"
]
